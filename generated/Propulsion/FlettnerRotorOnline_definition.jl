### DO NOT EDIT THIS FILE
### This file is auto-generated by the Dyad command-line compiler.
### If you edit this code it is likely to get overwritten.
### Instead, update the Dyad source code and regenerate this file


import Moshi as __Ext__Moshi

@doc Markdown.doc"""
   FlettnerRotorOnline(; name, R, H, MaxOmega)

Live-WaterLily.jl co-simulation variant of `FlettnerRotor`. The Cl(ξ) / Cd(ξ)
table lookup is replaced by two registered Julia functions
`DyadShip.flettner_live_fx(time)` and `DyadShip.flettner_live_fy(time)`
that return the latest body-frame rotor force computed by an external
WaterLily simulation. The simulation is advanced by a `PeriodicCallback`
installed by the caller (see `scripts/run_flettner_cosim_verification.jl`).

**Verification only.** A long Dyad transient calls the rhs O(10⁵) times;
WaterLily steps cost ~10–100 ms each. Per-rhs CFD is intractable. This
component exists to validate that, at a given coupling step Δt (typically
0.1–0.5 s), the live CFD forces are consistent with the offline-characterized
Cl/Cd table used by the production `FlettnerRotor`. Stick to short
analyses (≤ 120 s) and treat results as ground-truth references rather
than operational simulations.

The interface is *almost* the same as `FlettnerRotor`, with two differences:
- The apparent wind enters as **magnitude + angle** (`ApparentWindSpeed`,
  `ApparentWindAngle`), not as the body-frame components — the callback
  reads these directly to size the WaterLily inflow / spin ratio without
  having to rebuild atan2's.
- `Cl`, `Cd`, `Lift`, `Drag` are not exposed — they live inside the
  WaterLily driver (`DyadShip.FlettnerCFDLive.STATE`) and the caller's
  callback / verification script can inspect them there.

Inputs:
- `Omega_Order` — commanded rotor angular velocity [rad/s]; sign chooses
  the Magnus direction.
- `ApparentWindSpeed` — magnitude of apparent wind on the rotor [m/s].
- `ApparentWindAngle` — apparent-wind attack angle in body frame [rad],
  same convention as `ApparentSpeedXY.AttackAngleSigned`.

Outputs:
- `Omega` — actual ω after `MaxOmega` clamp.
- `Force_X`, `Force_Y` — body-frame force (forwarded from the live CFD
  state, held constant between coupling-step callbacks).
- `U_app_obs`, `alpha_obs`, `omega_obs` — passthrough observables the
  PeriodicCallback reads to drive WaterLily.

The `frame_a` hookup follows the same body→world rotation pattern as
`FlettnerRotor` / `WingSail`.

## Parameters:

| Name         | Description                         | Units  |   Default value |
| ------------ | ----------------------------------- | ------ | --------------- |
| `R`         |                          | m  |   2.5 |
| `H`         |                          | m  |   24 |
| `MaxOmega`         |                          | --  |   250 |

## Connectors

 * `Omega_Order` - This connector represents a real signal as an input to a component ([`RealInput`](@ref))
 * `ApparentWindSpeed` - This connector represents a real signal as an input to a component ([`RealInput`](@ref))
 * `ApparentWindAngle` - This connector represents a real signal as an input to a component ([`RealInput`](@ref))
 * `frame_a` - Coordinate system (2-dim.) fixed to the component with one cut-force and cut-torque.
All variables are resolved in the planar world frame. ([`Frame2D`](@ref))
 * `Omega` - This connector represents a real signal as an output from a component ([`RealOutput`](@ref))
 * `Force_X` - This connector represents a real signal as an output from a component ([`RealOutput`](@ref))
 * `Force_Y` - This connector represents a real signal as an output from a component ([`RealOutput`](@ref))
 * `U_app_obs` - This connector represents a real signal as an output from a component ([`RealOutput`](@ref))
 * `alpha_obs` - This connector represents a real signal as an output from a component ([`RealOutput`](@ref))
 * `omega_obs` - This connector represents a real signal as an output from a component ([`RealOutput`](@ref))
"""
@component function FlettnerRotorOnline(; name = nothing, R=2.5, H=Float64(24), MaxOmega=Float64(250), kwargs...)
  isnothing(name) && throw(ArgumentError("""
    The `name` keyword must be provided. Please consider using the `@named` macro,
    like so:
  
    @named model = FlettnerRotorOnline()
  """))

  __overrides = __build_overrides(kwargs)
  __params = Symbolics.SymbolicT[]
  __vars = Symbolics.SymbolicT[]
  __systems = System[]
  __guesses = Dict{Symbolics.SymbolicT, Symbolics.SymbolicT}()
  __initial_conditions = Dict{Symbolics.SymbolicT, Symbolics.SymbolicT}()
  __initialization_eqs = Equation[]
  __eqs = Equation[]
  __bindings = Dict{Symbolics.SymbolicT, Symbolics.SymbolicT}()

  ### Structural Parameters (functions)

  ### Structural Parameters (Final)

  ### Path Parameters (functions)

  ### Path Parameters (non-final)

  ### Final Parameters (declarations)

  ### Deferred assignment (default values that depend on final parameters)

  ### Symbolic Parameters
  __local__R = R
  append!(__params, @parameters (R::Real))
  __initial_conditions[R] = __local__R
  __local__H = H
  append!(__params, @parameters (H::Real))
  __initial_conditions[H] = __local__H
  __local__MaxOmega = MaxOmega
  append!(__params, @parameters (MaxOmega::Real))
  __initial_conditions[MaxOmega] = __local__MaxOmega

  ### Final Parameters (assignments)

  ### Final Path Parameters
  append!(__vars, @variables (Omega_Order(t)::Real), [input = true])
  append!(__vars, @variables (ApparentWindSpeed(t)::Real), [input = true])
  append!(__vars, @variables (ApparentWindAngle(t)::Real), [input = true])
  append!(__vars, @variables (Omega(t)::Real), [output = true])
  append!(__vars, @variables (Force_X(t)::Real), [output = true])
  append!(__vars, @variables (Force_Y(t)::Real), [output = true])
  append!(__vars, @variables (U_app_obs(t)::Real), [output = true])
  append!(__vars, @variables (alpha_obs(t)::Real), [output = true])
  append!(__vars, @variables (omega_obs(t)::Real), [output = true])

  ### Variables (declarations)

  ### Variables (assignments)

  ### Constants
  __constants = Any[]

  ### Components
  push!(__systems, @named frame_a = __Dyad__Frame2D())

  ### Check there are no unmatched overrides
  isempty(__overrides) || throw(ArgumentError("overides: [$(join(keys(__overrides), ", "))] don't match names found in model. These names may exist in the model but could have been conditionally excluded."))

  ### Guesses

  ### Initialization Equations

  ### Assertions
  __assertions = []

  ### Equations
  push!(__eqs, Omega ~ clamp(Omega_Order, -MaxOmega, MaxOmega))
  push!(__eqs, omega_obs ~ Omega)
  push!(__eqs, U_app_obs ~ ApparentWindSpeed)
  push!(__eqs, alpha_obs ~ ApparentWindAngle)
  push!(__eqs, Force_X ~ DyadShip.flettner_live_fx(t))
  push!(__eqs, Force_Y ~ DyadShip.flettner_live_fy(t))
  push!(__eqs, frame_a.fx + cos(frame_a.phi) * Force_X - sin(frame_a.phi) * Force_Y ~ 0)
  push!(__eqs, frame_a.fy + sin(frame_a.phi) * Force_X + cos(frame_a.phi) * Force_Y ~ 0)
  push!(__eqs, frame_a.tau ~ 0)

  # Return completely constructed System
  return System(__eqs, t, __vars, __params; systems=__systems, initial_conditions=__initial_conditions, guesses=__guesses, name, initialization_eqs=__initialization_eqs, bindings=__bindings, assertions=__assertions)
end
export FlettnerRotorOnline
