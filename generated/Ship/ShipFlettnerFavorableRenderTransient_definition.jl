### DO NOT EDIT THIS FILE
### This file is auto-generated by the Dyad command-line compiler.
### If you edit this code it is likely to get overwritten.
### Instead, update the Dyad source code and regenerate this file


using DyadInterface
using DyadInterface: ODEAlg, DEVerbosity, OptimizationLevel
using ModelingToolkit: SymbolicT, toggle_namespacing
using DyadInterface: AbstractTransientAnalysisSpec, TransientAnalysisSpec
@kwdef mutable struct ShipFlettnerFavorableRenderTransientSpec <: AbstractTransientAnalysisSpec
  name::Symbol = :ShipFlettnerFavorableRenderTransient
  var"alg"::ODEAlg.Type = ODEAlg.Auto()
  var"start"::Float64 = 0
  var"stop"::Float64 = 1500.0
  var"abstol"::Float64 = 0.000001
  var"reltol"::Float64 = 0.000001
  var"saveat"::Float64 = 0
  var"dtmax"::Float64 = 0
  var"tstops"::Array{Float64, 1} = []
  var"automatic_discontinuity_detection"::Bool = false
  var"optimize"::OptimizationLevel.Type = OptimizationLevel.None()
  var"progress"::Bool = true
  var"respecialize"::Bool = false
  var"verbose"::DEVerbosity.Type = DEVerbosity.Standard()
  var"log_file"::String = ""
  # Flettner-rotor ship with a *favorable* wind orientation. Same hull / propulsion /
  # autopilot / rotor stack as `FullShipFlettnerRender`, but the mean wind direction
  # is rotated so the apparent wind hits the ship broadside, putting the Magnus
  # lift squarely along the surge axis instead of mostly into sway.
  # 
  # Physics: the standard Flettner sweet spot is beam wind, with the rotor's spin
  # direction chosen so lift points forward. For an east-bound ship (target +X)
  # with the default CCW spin (ω > 0), wind blowing southward (i.e. `WindDirection
  # = 0`, from the north) produces:
  # - body-frame wind ≈ (−tiny, −U_app)   [port beam]
  # - ex = 0,  ey ≈ −1
  # - Force_X = −sign_omega · Lift · ey ≈ +Lift   (forward thrust)
  # - Force_Y ≈ −Drag                              (small, southward)
  # 
  # So the rotor contributes its full lift coefficient to surge thrust and only
  # the small Cd-scaled drag to sway. Compared with `FullShipFlettnerRender`
  # (wind mean 45°, ~30% of |F| in surge), this variant gets ~90% of |F| in
  # surge, and the autopilot's job stays inside its linear regime through the
  # whole horizon.
  # 
  # Everything else (rotor geometry, ramp profile, target, autopilot gains) is
  # identical so the three-way comparison (no-rotor / unfavorable / favorable) is
  # purely about the wind direction.
  var"model"::Union{Nothing, System} = DyadShip.Ship.FullShipFlettnerFavorableRender(; name=:FullShipFlettnerFavorableRender)
end

function DyadInterface.run_analysis(spec::ShipFlettnerFavorableRenderTransientSpec)
  overrides = Dict{SymbolicT, SymbolicT}()
  no_namespace_model = toggle_namespacing(spec.model, false)
  base_spec = TransientAnalysisSpec(;
    name=:TransientAnalysis, overrides, alg=spec.alg, start=spec.start, stop=spec.stop, abstol=spec.abstol, reltol=spec.reltol, saveat=spec.saveat, dtmax=spec.dtmax, tstops=spec.tstops, automatic_discontinuity_detection=spec.automatic_discontinuity_detection, optimize=spec.optimize, progress=spec.progress, respecialize=spec.respecialize, verbose=spec.verbose, log_file=spec.log_file, model=spec.model
  )
  run_analysis(base_spec)
end

ShipFlettnerFavorableRenderTransient(;kwargs...) = run_analysis(ShipFlettnerFavorableRenderTransientSpec(;kwargs...))
export ShipFlettnerFavorableRenderTransient, ShipFlettnerFavorableRenderTransientSpec
