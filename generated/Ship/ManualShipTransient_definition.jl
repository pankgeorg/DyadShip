### DO NOT EDIT THIS FILE
### This file is auto-generated by the Dyad command-line compiler.
### If you edit this code it is likely to get overwritten.
### Instead, update the Dyad source code and regenerate this file


using DyadInterface
using DyadInterface: ODEAlg, DEVerbosity, OptimizationLevel
using ModelingToolkit: SymbolicT, toggle_namespacing
using DyadInterface: AbstractTransientAnalysisSpec, TransientAnalysisSpec
@kwdef mutable struct ManualShipTransientSpec <: AbstractTransientAnalysisSpec
  name::Symbol = :ManualShipTransient
  var"alg"::ODEAlg.Type = ODEAlg.Auto()
  var"start"::Float64 = 0
  var"stop"::Float64 = 600.0
  var"abstol"::Float64 = 0.000001
  var"reltol"::Float64 = 0.000001
  var"saveat"::Float64 = 0
  var"dtmax"::Float64 = 0
  var"tstops"::Array{Float64, 1} = []
  var"automatic_discontinuity_detection"::Bool = false
  var"optimize"::OptimizationLevel.Type = OptimizationLevel.None()
  var"progress"::Bool = true
  var"respecialize"::Bool = false
  var"verbose"::DEVerbosity.Type = DEVerbosity.Standard()
  var"log_file"::String = ""
  # Open-loop, human-controllable ship: hull + propeller + rudder, no autopilot.
  # 
  # The user inputs are the tunable parameters `rpm_cmd` (commanded shaft speed [rpm]) and
  # `rudder_cmd` (commanded rudder angle [deg]). Each command feeds a first-order *input
  # state* (engine-telegraph / steering-gear lag) so live command changes arrive smoothly:
  # `der(rpm_state) = (rpm_cmd - rpm_state) / T_rpm` and likewise for the rudder. The shaft
  # is driven by a filtered `SpeedSource` tracking `rpm_state` (ideal governor — load torque
  # does not stall the engine), and `rudder_state` becomes the rudder order (the `Rudder`
  # component applies its own ±`MaxRudderAngle` clamp and actuator lag). Intended for
  # interactive (WASM model-exchange) use where `rpm_cmd`/`rudder_cmd` are re-tuned between
  # integration steps; also works as a plain transient analysis.
  var"model"::Union{Nothing, System} = DyadShip.Ship.ManualShip(; name=:ManualShip)
end

function DyadInterface.run_analysis(spec::ManualShipTransientSpec)
  overrides = Dict{SymbolicT, SymbolicT}()
  no_namespace_model = toggle_namespacing(spec.model, false)
  
  base_spec = TransientAnalysisSpec(;
    name=:TransientAnalysis, overrides, alg=spec.alg, start=spec.start, stop=spec.stop, abstol=spec.abstol, reltol=spec.reltol, saveat=spec.saveat, dtmax=spec.dtmax, tstops=spec.tstops, automatic_discontinuity_detection=spec.automatic_discontinuity_detection, optimize=spec.optimize, progress=spec.progress, respecialize=spec.respecialize, verbose=spec.verbose, log_file=spec.log_file, model=spec.model
  )
  run_analysis(base_spec)
end

ManualShipTransient(;kwargs...) = run_analysis(ManualShipTransientSpec(;kwargs...))
export ManualShipTransient, ManualShipTransientSpec
