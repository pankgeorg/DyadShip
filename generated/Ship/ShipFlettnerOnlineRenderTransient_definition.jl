### DO NOT EDIT THIS FILE
### This file is auto-generated by the Dyad command-line compiler.
### If you edit this code it is likely to get overwritten.
### Instead, update the Dyad source code and regenerate this file


using DyadInterface
using DyadInterface: ODEAlg, DEVerbosity, OptimizationLevel
using ModelingToolkit: SymbolicT, toggle_namespacing
using DyadInterface: AbstractTransientAnalysisSpec, TransientAnalysisSpec
@kwdef mutable struct ShipFlettnerOnlineRenderTransientSpec <: AbstractTransientAnalysisSpec
  name::Symbol = :ShipFlettnerOnlineRenderTransient
  var"alg"::ODEAlg.Type = ODEAlg.Auto()
  var"start"::Float64 = 0
  var"stop"::Float64 = 90.0
  var"abstol"::Float64 = 0.000001
  var"reltol"::Float64 = 0.000001
  var"saveat"::Float64 = 0
  var"dtmax"::Float64 = 0
  var"tstops"::Array{Float64, 1} = []
  var"automatic_discontinuity_detection"::Bool = false
  var"optimize"::OptimizationLevel.Type = OptimizationLevel.None()
  var"progress"::Bool = true
  var"respecialize"::Bool = false
  var"verbose"::DEVerbosity.Type = DEVerbosity.Standard()
  var"log_file"::String = ""
  # Short ship transient with the **live-WaterLily** `FlettnerRotorOnline`
  # component substituting the table-driven `FlettnerRotor`. Geometry,
  # autopilot gains, hull params, mean wind direction (`0°`, beam from port —
  # the favorable case), and rotor R/H/ω target are all identical to
  # `FullShipFlettnerFavorableRender`, so the per-time-point force trace can
  # be compared head-to-head with the table-based version to validate the
  # coupling.
  # 
  # Stops at `t = 90 s` — long enough for the rotor to ramp up (`τ = 30 s`)
  # and the ship to start making real headway, short enough that the
  # WaterLily callback cost (one `sim_step!` per coupling Δt ≈ 0.2 s ⇒ ~450
  # CFD steps total) is bearable.
  # 
  # The caller is responsible for:
  # 1. Initializing the driver: `DyadShip.FlettnerCFDLive.init!(R=..., H=..., ...)`
  #    (requires `using WaterLily` in the active env).
  # 2. Installing a `DiffEqCallbacks.PeriodicCallback(...)` that reads
  #    `rotor.U_app_obs`, `rotor.alpha_obs`, `rotor.omega_obs` from the
  #    integrator and calls `DyadShip.FlettnerCFDLive.step!(...)`.
  # 3. Solving with a non-AD Jacobian (the registered force functions don't
  #    carry derivative information).
  # 
  # See `scripts/run_flettner_cosim_verification.jl` for the end-to-end driver.
  var"model"::Union{Nothing, System} = DyadShip.Ship.FullShipFlettnerOnlineRender(; name=:FullShipFlettnerOnlineRender)
end

function DyadInterface.run_analysis(spec::ShipFlettnerOnlineRenderTransientSpec)
  overrides = Dict{SymbolicT, SymbolicT}()
  no_namespace_model = toggle_namespacing(spec.model, false)
  base_spec = TransientAnalysisSpec(;
    name=:TransientAnalysis, overrides, alg=spec.alg, start=spec.start, stop=spec.stop, abstol=spec.abstol, reltol=spec.reltol, saveat=spec.saveat, dtmax=spec.dtmax, tstops=spec.tstops, automatic_discontinuity_detection=spec.automatic_discontinuity_detection, optimize=spec.optimize, progress=spec.progress, respecialize=spec.respecialize, verbose=spec.verbose, log_file=spec.log_file, model=spec.model
  )
  run_analysis(base_spec)
end

ShipFlettnerOnlineRenderTransient(;kwargs...) = run_analysis(ShipFlettnerOnlineRenderTransientSpec(;kwargs...))
export ShipFlettnerOnlineRenderTransient, ShipFlettnerOnlineRenderTransientSpec
