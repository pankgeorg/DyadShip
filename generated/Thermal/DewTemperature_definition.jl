### DO NOT EDIT THIS FILE
### This file is auto-generated by the Dyad command-line compiler.
### If you edit this code it is likely to get overwritten.
### Instead, update the Dyad source code and regenerate this file


@doc Markdown.doc"""
   DewTemperature(; name)

Dew-point temperature from a saturation pressure and relative humidity input.

Ported from `ShipSIM.Components.Others.MoistAir.DewTemperature`. The original Modelica model
read a moist-air `port` and used `Modelica.Media`'s saturation pressure / relative humidity
calls. Dyad has no equivalent fluid medium, so this version is signal-based: callers supply
the saturation pressure `P_sat` (Pa) and relative humidity `RH` (0..1) directly, and the
component returns the dew-point temperature in Kelvin using Magnus' formula:

```math
T_\mathrm{dew} = 273.15 + \frac{241.88 \, \ln(P_\mathrm{vap} / 610.78)}{17.558 - \ln(P_\mathrm{vap} / 610.78)}
```

with `P_vap = P_sat · RH`.

This is the same formula as in the upstream model, only the data plumbing is different.

## Connectors

 * `P_sat` - This connector represents a real signal as an input to a component ([`RealInput`](@ref))
 * `RH` - This connector represents a real signal as an input to a component ([`RealInput`](@ref))
 * `Tdew` - This connector represents a real signal as an output from a component ([`RealOutput`](@ref))

## Variables

| Name         | Description                         | Units  | 
| ------------ | ----------------------------------- | ------ | 
| `P_vap`         |                          | Pa  | 
"""
@component function DewTemperature(; name = nothing, kwargs...)
  isnothing(name) && throw(ArgumentError("""
        The `name` keyword must be provided. Please consider using the `@named` macro,
        like so:

        @named model = DewTemperature()
        """))
  __overrides = Dict{String, Symbolics.SymbolicT}(string(k) => v for (k, v) in kwargs)
  __params = Symbolics.SymbolicT[]
  __vars = Symbolics.SymbolicT[]
  __systems = System[]
  __guesses = Dict{Symbolics.SymbolicT, Symbolics.SymbolicT}()
  __initial_conditions = Dict{Symbolics.SymbolicT, Symbolics.SymbolicT}()
  __initialization_eqs = Equation[]
  __eqs = Equation[]
  __bindings = Dict{Symbolics.SymbolicT, Symbolics.SymbolicT}()

  ### Structural Parameters (functions)

  ### Structural Parameters (Final)

  ### Path Parameters (functions)

  ### Path Parameters (non-final)

  ### Final Parameters (declarations)

  ### Final Parameters (assignments)

  ### Deferred assignment (default values that depend on final parameters)

  ### Symbolic Parameters

  ### Final Path Parameters
  append!(__vars, @variables (P_sat(t)::Real), [input = true])
  append!(__vars, @variables (RH(t)::Real), [input = true])
  append!(__vars, @variables (Tdew(t)::Real), [output = true])

  ### Variables (declarations)
  append!(__vars, @variables (P_vap(t)::Real))

  ### Variables (assignments)
  __ovr_P_vap = pop!(__overrides, "P_vap", nothing); isnothing(__ovr_P_vap) || push!(__eqs, P_vap ~ __ovr_P_vap)
  __ovr_P_vap__initial = pop!(__overrides, "P_vap__initial", nothing); isnothing(__ovr_P_vap__initial) || (__initial_conditions[P_vap] = __ovr_P_vap__initial)

  ### Constants
  __constants = Any[]

  ### Components

  ### Check there are no unmatched overrides
  isempty(__overrides) || throw(ArgumentError("overides: [$(join(keys(__overrides), ", "))] don't match names found in model. These names may exist in the model but could have been conditionally excluded."))

  ### Guesses

  ### Initialization Equations

  ### Assertions
  __assertions = []

  ### Equations
  push!(__eqs, P_vap ~ P_sat * RH)
  push!(__eqs, Tdew ~ 273.15 + (241.88 * log(P_vap / 610.78)) / (17.558 - log(P_vap / 610.78)))

  # Return completely constructed System
  return System(__eqs, t, __vars, __params; systems=__systems, initial_conditions=__initial_conditions, guesses=__guesses, name, initialization_eqs=__initialization_eqs, bindings=__bindings, assertions=__assertions)
end
export DewTemperature
