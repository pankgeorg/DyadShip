### DO NOT EDIT THIS FILE
### This file is auto-generated by the Dyad command-line compiler.
### If you edit this code it is likely to get overwritten.
### Instead, update the Dyad source code and regenerate this file


@doc Markdown.doc"""
   SolarIrradiation(; name, latitude, longitude, time_zone, day_of_year, irradiance_ref)

Solar irradiation for a fixed geographic location and date — a clear-sky model.

Ported from `ShipSIM.Components.Others.Solar.SolarIrradiation`.

The original Modelica model:
- Maintained a `discrete Integer dayOfYear` that incremented via `when` events when local
  hour wrapped past 24.
- Read an optional `tableOnFile` of measured irradiance to compute a cloud-correction
  ratio (`SolarIrradiationRatio`).
- Provided three signals: `IrradianceOnPanel(W/m²)`, `SunHeight(deg)`, `SunVector[3]`.

This Dyad port:
- Drops the discrete day-of-year counter and `when` event; the day of the year is fixed
  per simulation via `day_of_year` (1..365). The starting reference is treated as
  midnight of that day in the local time zone.
- Drops the optional measured-irradiance table; the output is the clear-sky reference
  irradiance directly. If the user wants cloud correction, multiply `IrradianceOnPanel`
  by an externally-supplied `SolarIrradiationRatio` real input downstream.
- Uses Julia helpers `DyadShip.sun_height_deg(...)` and `DyadShip.sun_vector_world(...)`
  which encode the same astronomy formulae as upstream (declination, true mean time,
  hour angle, altitude, azimuth).
- Exposes `IrradianceOnPanel` only when `Calculation_on_Panel = true` AND the panel
  inclination + azimuth inputs are wired. For now this port always uses a horizontal
  panel; an inclined-plane variant follows the same shape.

Limitations:
- Astronomy formulae preserved verbatim; no atmospheric extinction model beyond the
  reference irradiance scale.
- 3-vector inputs/outputs are exposed as triples of scalar `RealOutput`s, since Dyad
  does not have array connectors.

## Parameters: 

| Name         | Description                         | Units  |   Default value |
| ------------ | ----------------------------------- | ------ | --------------- |
| `latitude`         | Latitude in radians (positive North). Default ≈ 43.4° N (Galicia, Spain).                         | rad  |   0.7579392485634024 |
| `longitude`         | Longitude in radians (positive East). Default ≈ -8.24° (Galicia).                         | rad  |   -0.1438785943756596 |
| `time_zone`         | Time zone offset from UTC in hours.                         | --  |   1 |
| `day_of_year`         | Day of year (1..365). Defaults to summer solstice.                         | --  |   172 |
| `irradiance_ref`         | Reference clear-sky horizontal irradiance [W/m²].                         | --  |   1000 |

## Connectors

 * `SunHeight` - This connector represents a real signal as an output from a component ([`RealOutput`](@ref))
 * `SunVector_x` - This connector represents a real signal as an output from a component ([`RealOutput`](@ref))
 * `SunVector_y` - This connector represents a real signal as an output from a component ([`RealOutput`](@ref))
 * `SunVector_z` - This connector represents a real signal as an output from a component ([`RealOutput`](@ref))
 * `IrradianceOnPanel` - This connector represents a real signal as an output from a component ([`RealOutput`](@ref))
 * `IsDay` - This connector represents a real signal as an output from a component ([`RealOutput`](@ref))

## Variables

| Name         | Description                         | Units  | 
| ------------ | ----------------------------------- | ------ | 
| `sv`         |                          | --  | 
"""
@component function SolarIrradiation(; name = nothing, latitude=0.7579392485634024, longitude=-0.1438785943756596, time_zone=Float64(1), day_of_year=Float64(172), irradiance_ref=Float64(1000), kwargs...)
  isnothing(name) && throw(ArgumentError("""
        The `name` keyword must be provided. Please consider using the `@named` macro,
        like so:

        @named model = SolarIrradiation()
        """))
  __overrides = Dict{String, Symbolics.SymbolicT}(string(k) => v for (k, v) in kwargs)
  __params = Symbolics.SymbolicT[]
  __vars = Symbolics.SymbolicT[]
  __systems = System[]
  __guesses = Dict{Symbolics.SymbolicT, Symbolics.SymbolicT}()
  __initial_conditions = Dict{Symbolics.SymbolicT, Symbolics.SymbolicT}()
  __initialization_eqs = Equation[]
  __eqs = Equation[]
  __bindings = Dict{Symbolics.SymbolicT, Symbolics.SymbolicT}()

  ### Structural Parameters (functions)

  ### Structural Parameters (Final)

  ### Path Parameters (functions)

  ### Path Parameters (non-final)

  ### Final Parameters (declarations)

  ### Final Parameters (assignments)

  ### Deferred assignment (default values that depend on final parameters)

  ### Symbolic Parameters
  __local__latitude = latitude
  append!(__params, @parameters (latitude::Real), [description = "Latitude in radians (positive North). Default ≈ 43.4° N (Galicia, Spain)."])
  __initial_conditions[latitude] = __local__latitude
  __local__longitude = longitude
  append!(__params, @parameters (longitude::Real), [description = "Longitude in radians (positive East). Default ≈ -8.24° (Galicia)."])
  __initial_conditions[longitude] = __local__longitude
  __local__time_zone = time_zone
  append!(__params, @parameters (time_zone::Real), [description = "Time zone offset from UTC in hours."])
  __initial_conditions[time_zone] = __local__time_zone
  __local__day_of_year = day_of_year
  append!(__params, @parameters (day_of_year::Real), [description = "Day of year (1..365). Defaults to summer solstice."])
  __initial_conditions[day_of_year] = __local__day_of_year
  __local__irradiance_ref = irradiance_ref
  append!(__params, @parameters (irradiance_ref::Real), [description = "Reference clear-sky horizontal irradiance [W/m²]."])
  __initial_conditions[irradiance_ref] = __local__irradiance_ref

  ### Final Path Parameters
  append!(__vars, @variables (SunHeight(t)::Real), [output = true])
  append!(__vars, @variables (SunVector_x(t)::Real), [output = true])
  append!(__vars, @variables (SunVector_y(t)::Real), [output = true])
  append!(__vars, @variables (SunVector_z(t)::Real), [output = true])
  append!(__vars, @variables (IrradianceOnPanel(t)::Real), [output = true])
  append!(__vars, @variables (IsDay(t)::Real), [output = true])

  ### Variables (declarations)
  append!(__vars, @variables (sv(t)[1:3]::Real))

  ### Variables (assignments)
  __ovr_sv = pop!(__overrides, "sv", nothing); isnothing(__ovr_sv) || push!(__eqs, sv ~ __ovr_sv)
  __ovr_sv__initial = pop!(__overrides, "sv__initial", nothing); isnothing(__ovr_sv__initial) || (__initial_conditions[sv] = __ovr_sv__initial)

  ### Constants
  __constants = Any[]

  ### Components

  ### Check there are no unmatched overrides
  isempty(__overrides) || throw(ArgumentError("overides: [$(join(keys(__overrides), ", "))] don't match names found in model. These names may exist in the model but could have been conditionally excluded."))

  ### Guesses

  ### Initialization Equations

  ### Assertions
  __assertions = []

  ### Equations
  push!(__eqs, SunHeight ~ sun_height_deg(t, latitude, longitude, day_of_year, time_zone))
  push!(__eqs, sv ~ sun_vector_world(t, latitude, longitude, day_of_year, time_zone, irradiance_ref))
  push!(__eqs, SunVector_x ~ sv[1])
  push!(__eqs, SunVector_y ~ sv[2])
  push!(__eqs, SunVector_z ~ sv[3])
  push!(__eqs, IrradianceOnPanel ~ ifelse(SunHeight > 0, -sv[3], 0))
  push!(__eqs, IsDay ~ ifelse(SunHeight > 0, 1, 0))

  # Return completely constructed System
  return System(__eqs, t, __vars, __params; systems=__systems, initial_conditions=__initial_conditions, guesses=__guesses, name, initialization_eqs=__initialization_eqs, bindings=__bindings, assertions=__assertions)
end
export SolarIrradiation
